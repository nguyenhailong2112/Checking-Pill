# Real Test Images

Put local demo/test images here.

Supported image extensions:

```text
.jpg, .jpeg, .png, .bmp, .webp
```

Run batch inference:

```powershell
python -m edgevision.cli batch --input data\real_test_images --output runs\real_test --config configs\runtime_yolo_config.json
```

